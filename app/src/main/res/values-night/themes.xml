<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema oscuro nativo para ventana/splash — el UI real es 100% Compose -->
    <style name="Theme.Denuncia" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/verde_claro</item>
        <item name="colorPrimaryDark">@color/fondo_oscuro</item>
        <item name="colorAccent">@color/verde_suave</item>
        <item name="android:colorBackground">@color/fondo_oscuro</item>
        <item name="android:statusBarColor">@color/fondo_oscuro</item>
        <item name="android:navigationBarColor">@color/fondo_oscuro</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

</resources>
